# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
*~
*.swp
.vscode/
.idea/

# local backups / scratch
*.bak
*.tmp
*-BACKUP.*
* - Copy.*

# pricing-impact baseline (generated from git HEAD, never committed)
tools/_baseline.html

# per-session writer lock (see AGENTS.md session protocol)
.session.lock

# Claude Code local-only settings
.claude/settings.local.json
