# Kabacal — STATUS (you are here)

**Update when:** a risk opens/closes, a decision is made, or in-flight work changes. Keep under ~60 lines — this is the mutable "current state", `ROADMAP.md` is the append-only history.
Last update: 2026-07-15 · SYNTEC Tier 2 (g) — opt-in () comments in the NC (header + per-op) + regenerated stale TPL goldens (a06702e).

## Open risks (ranked)

1. **The .NC has never cut real material.** The portrait machine frame (`tpXform`, x=1220−y) is validated on file diffs vs VCarve only. Goldens are "known state", not "known good". → Do the air-cut protocol (Z+50 air pass → scrap sheet → compare with a VCarve-cut part) **before more CAM features**, then mark goldens known-good in `tests/golden/README.md`.
2. ~~Toolpath scope staleness~~ **CLOSED 2026-07-08 (`e37cf52`)**: scoped toolpaths carry a signature of their source items (`pp.sig`); on any mismatch (delete/resize/split) `tpPathParts` cuts NOTHING and the tree shows an amber STALE badge + "Remove stale". Verified: delete under 7 template paths → 7 STALE, 0 moves emitted. `loadFastCnc` now always resets the tree before restoring the file's own paths.
3. **Pricing/business config lives in one browser profile.** `kab_prices`, `kab_custom_mats`, `kab_pricecfg`, `kab_company`, `kab_tooldb`, `kab_panels` have no automatic backup (manual Save-settings export exists). Profile reset = silent revert to book prices. → **Mitigation shipped 2026-07-11**: SaaS Phase 3 ⇧ Push settings to cloud (docs/SAAS.md) — becomes real the moment Ednei signs in and pushes once; risk closes fully when pushing is habitual (or a later auto-push). `kab_panels` (current rooms) travels in `.fastcnc`, not settings.
4. **Repo and live app are PUBLIC.** The price book, £75 rule, and website formula are readable by anyone (repo + view-source on Pages). → Decision needed: private hosting vs consciously accepted risk. Write the decision here either way. Full exposure audit + tenant-side answer now in docs/SAAS.md §Sensitive-config (shops' real numbers go to RLS-protected `account_settings` at Phase 3; genericising the embedded FAST CNC book = open decision D2).
5. **Push = live, CI alarms only afterwards.** A broken push is production for ~1 minute and stays until noticed. → Switch Pages to deploy-from-Actions gated on `check.mjs`.

## In flight

- **SYNTEC Tier 2 — opt-in NC comments** shipped 2026-07-15 (ROADMAP *g*): a checkbox in the Save-NC modal (`camJob.ncComments`, default OFF) makes `ncPegasus(segs,meta)` emit `()` production comments — a header after `:1248` (JOB/NC_FILE/CLIENT/MATERIAL/THICKNESS/SHEET_SIZE/PARTS/DATUM/Z_ZERO/CREATED) + one line per tool segment with the real toolpath names. **Comments never consume an N-number, so the machining program is byte-identical with/without them** (proven: strip the `()` lines → equals the OFF output). ASCII/parens/accents sanitised; `:1248` untouched. Default OFF keeps every NC golden byte-identical. **Air-cut mandatory before production** (the SYNTEC must accept `()`). Next: Tier 3 (ZPL/Zebra), Tier 4 (est. time — a `tpStats` cost model already exists to build on). **Integrity note: also regenerated `GOLDEN_TPL_S1/S2`** — they'd been stale since the Fork's `a06702e` flushback all-live safety fix (it changed the output but only re-checked the ll/c goldens); new values verified safe (minZ=0, no negative Z).
- **SYNTEC Tier 1 — production package** shipped 2026-07-15 (ROADMAP *f*): "⤓ Production package (.zip)" button in the NC export modal bundles **one .zip per order** (`{ORDER}/` folder) with every sheet NC (exact `ncPegasus` output) + `{ORDER}_labels.html` (reused A4 labels, with QR) + `{ORDER}_manifest.csv` (one row per part; order/nc_file/sheet/material/thickness/size/part_no/name/W/H/role/uid). Same order ID on the folder, all NC names, labels and manifest → SYNTEC **ScanMode/Workinglist** and traceability line up (the ScanMode barcode = the NC filename). Inline store-only `zipStore` (CRC32, no dependency). **Pure packaging — NC bytes unchanged, goldens byte-identical.** First of 4 SYNTEC tiers; next: Tier 2 (opt-in `()` comments in the NC + air-cut), Tier 3 (ZPL/Zebra), Tier 4 (est. time). NC-comment/`:1248` deliberately untouched until validated on the machine.
- **Panels no Quote/PDF** shipped 2026-07-15 (ROADMAP *e*): job com Wall Panels ganha seção "Wall Panels" no Quote (tela) E PDF — resumo (Total panels / **Panel material area** = Σ(w×h)/1e6 / **Painted front area** = Σ(w×h−janelas)/1e6), panorama que **esconde paredes sem painel** (só no Quote/PDF; editor mostra todas), sheet preview, e cut list (Wall/Panel ID/W/H/Thick/Area, nomes Wall 1/Wall 1A). Dois toggles no Quote controlam tela+PDF (default ON com painéis). Editor ganhou rodapé discreto de m². **Só display** — pricing/DXF/NC/geometria intocados; doors-only sai vazio. check.mjs verde; sandbox 30/30; **goldens DXF byte-idênticos** (PANELS_18mm 10030, WALL_LAYOUT 3428); só `index.html` mudou.
- **Flushback all-live + Plain Shaker template** shipped 2026-07-13 (ROADMAP *yy*): (1) Flushback templates (`tpl_flush18`/`tpl_flushins12`) now carry `allLive:true` so **every op ships LIVE** (`tplApply` forces live where there's geometry) — `GOLDEN_TPL_S1/S2` regenerated (1525→2788, 661→1174). (2) New preset-gated **Plain Shaker** template (`tpl_plainshaker22` + the 18mm twin `tpl_plainshaker18`), converted from the user's VCarve `.ToolpathTemplate` and validated structurally vs the reference `.nc`: T12 skim → T1 6mm finish → T2 2mm finish (3/6) → T1 OUT rebate (2.5/5/6) → T1 offcut through. Auto-applies on picking the Plain Shaker preset (all 5 ops live). The 18mm twin keeps the 6mm recess and only changes the offcut to the board depth. New goldens `GOLDEN_PLAINSHAKER_S1_{22,18}mm.nc`; all 18 goldens verified (12 identical, 2 regenerated, 2 new); every NC min-Z ≥ 0. **Air-cut still mandatory before real material.**
- **CAM auto-apply on selection + Ogee depth fix** shipped 2026-07-13 (ROADMAP *xx*): picking a door **type** (Flushback) or **offset preset** (Ogee) in the doors editor now **auto-applies the matching cut template** — no trip to the Toolpaths tab. New `tplAutoSyncItem(i)` (hooked into `applyProfile`/`upd`): idempotent + self-cleaning (never duplicates a template already covering the part, manual or auto; removes only its own `grp.fromSel` group when the match goes away), item-scoped so it never touches panel rooms (`tplApply` gained additive `idsOverride`/`fromSel`; manual/recipe applies unchanged → **14 goldens byte-identical**). A green "⚡ Cut ready" chip shows in the doors editor. Templates were never removed — they + the manual ⚡ Auto button always existed; the gap was the on-selection trigger. Also: **Ogee 6mm pocket-finish 11.8→11.5** (was 0.3mm below the pocket floor) — 2 Ogee goldens regenerated (`Z10.200`→`Z10.500`, 4/6 lines, sizes unchanged).
- **Nesting auto-upsize for oversize parts** shipped 2026-07-13 (ROADMAP *ww*): the auto-nester grouped by material and forced ONE sheet size per material (`sheetMeta` = `sheetSizeOv||matSizeDef||nestSize`), so a part longer/wider than the default sheet (e.g. a 2850mm window frame vs an 8×4=2440 sheet) was dumped at the corner and overflowed — James's file came out 6 sheets with 5 parts hanging off. Now `autoPack` groups a material's parts by the sheet size they need (default when it fits, else the smallest that does — `fitSheetSize`) and nests each group on its size; `materialize`/`buildSheetGroups` remember + honour that size (manual size override still wins). Uniform jobs = one group = **byte-identical** (all doors goldens verified). James now: 6→3 sheets, 0 overflow, small parts on 8×4, frames on 10×4. New nest test in check.mjs. Pricing is per-sheet so mixed sizes cost correctly. Also: the part-list frame chip now reads **T B L R + MR** (was T R B L, no mid-rail).
- **Multi-panel door editor consolidated** shipped 2026-07-13 (ROADMAP *uu*): the outer frame (T/B/L/R + link), internal rail (`midFrame`) and bottom-referenced panel size (`panelSize`) now sit together in the Parts editor for 2+ panel doors, with a "measured from the bottom" note. **Pure UI** — reuses existing setters, no geometry change, doors goldens byte-identical. Investigation first proved the model already did all of it (frame/rail/panel independent; bottom-referenced; DXF==preview); the only gap was the controls being split across the Part and Offset sections. Known non-gaps (not requested now): 3+ panels only size the bottom opening; one shared rail between all panels.
- **Paneling review** shipped 2026-07-13 (ROADMAP *tt*): two reported issues, both confirmed by numeric + E2E test before any edit. (1) **Rotated-panel left/right inversion was REAL** — `pnCellRects`'s rotated transform was a reflection (det −1) that swapped L/R on any panel the nester rotates; an asymmetric frame (door-side 175 vs neighbour 40) landed on the wrong edge in the DXF. One-term fix → proper 90° CW rotation, matched to the Doors `placedCavs` net (Doors never had the bug — proven by the byte-identical `GOLDEN_OGEE_S1_22mm.nc` control). (2) **Room Front View now draws the offset lines** (`pnPanoSvg` gained the same per-cell inset loop as `pnWallSvg` — real geometry). Only `GOLDEN_PANELS_18mm.dxf` moved (10038→10030, 60 `OFFSET_A` Y-coords on the 4 rotated pieces only; regenerated same commit); other 14 goldens byte-identical.
- **Panels toolpath hardening** shipped 2026-07-13 (ROADMAP *ss*): the two Panels→CAM pendings closed — panels paths now stale-track (`pp.pnSigs` content signature; room edit/delete → amber STALE + cuts nothing, re-apply the group) and the panels NC has a binary golden `GOLDEN_OGEE_PANELS_S1_22mm.nc`. Doors sig/goldens untouched; 15/15 old goldens byte-identical. **Ogee on wall panels is now regression-locked.** Air-cut still mandatory before real material.
- **URGENT Doors bugfix** shipped 2026-07-13 (ROADMAP *rr*): rotated tall doors no longer go flat — new `placedCavs(p)` computes cavities from LOGICAL w/h (frame follows width/height, not sheet orientation) and transposes for the rotated placement; used by app preview + DXF + CAM so they always agree. Also: Enter adds exactly one part (removed a duplicate global handler), qty clamped >=1 everywhere (mkItem/loadFastCnc/genParts + input min) — a qty-0 part no longer silently vanishes, frame editor reordered Top/Bottom/Left/Right. Ednei's `Issues James.fastcnc.json` loads with all 5 parts nesting + full Ogee offsets in the DXF; 15/15 goldens byte-identical.
- **Toolpath groups + faithful preview + PANELS CAM BRIDGE** shipped 2026-07-12 (ROADMAP *pp*+*qq*): template applies = coloured GROUPS (whole-group on/off, group delete, part highlighted in group colour); preview draws TRUE offset lines per layer colour + PROFILE section + per-TOOL colours with painted pocket areas and sweep bands; panels sheets are now toolpath sheets (cells as cavities, sheet-scoped ops only — doors jobs byte-identical, proven). Ednei's Ogee now machines WALL PANELS. Pending: panels-NC binary golden + stale-tracking for panels paths; air-cut still mandatory before real material.
- **Ogee VCarve-exactness round** shipped 2026-07-12 (ROADMAP *oo*): real-world tool-resolution bug fixed (saved kab_tooldb predated the new tools → wrong 2mm/6mm tools cut; boot now seeds missing factory ids + num+dia resolution), OUT = VCarve pattern (rough +0.4 / exact ramped final), pocket line rule + per-level serpentine reversal, V-bit CW from mid-right. GOLDEN_OGEE regenerated (38555), proven byte-identical via two independent paths; 14/14 old goldens untouched. **PENDING next session (Ednei's ask): toolpath GROUP UI (per-group on/off, bulk delete, group/tool colours, part highlighted in group colour), preview with true DXF lines + painted pocket areas, then the Panels CAM bridge.**
- **Ogee Moulding 22mm toolpath template** shipped 2026-07-12 (ROADMAP *mm*): preset-name-matched template (`tpl_ogee22`, gated th=22 + offsetName='Ogee', visible-but-blocked otherwise), new engine op kinds pocket-raster + profile-sweep (ball-projected Ogee section) + V-bit corner-sharpen/effective-radius + depthList/feed overrides, T11 ball 5mm + T12 50.8 skim added to toolDb (Ednei confirmed numbers). Structure-validated vs the VCarve reference NC; new golden `GOLDEN_OGEE_S1_22mm.nc`; 14/14 old goldens byte-identical. **Risk 1 (air-cut) now also covers this 3D op — do NOT cut real material before the air-cut protocol.** **NEXT BIG: Panels CAM bridge** (Ednei needs Ogee on wall panels; panels sheets → toolpath parts; the preset gating is already generic).
- **Preset polish** shipped 2026-07-12 (ROADMAP *ll*): UI label "Preset", factory built-ins None/**Plain Shaker**(renamed)/**Ogee**(embedded values+section, no import needed), "All offsets" removed, Save/Import as buttons; legacy names sanitised/mapped (`extProfiles`/`normPresetName`) so old files load unchanged. 14/14 goldens byte-identical.
- **Offset presets by DXF import (Ogee) + PROFILE layer** shipped 2026-07-12 (ROADMAP *kk*): named offset preset in the Offsets area for **Doors + Panels**; ⬆ Import DXF reads `OFFSET_A..G` (A = frame-zero, B–G = inward spacing read from the DXF, `OUT` ignored) + `PROFILE`; new additive `PROFILE` layer (177) drawn once per sheet (Doors `dxfForThickness` + Panels `pnDxfForThickness`) for profile-carrying presets; additive persistence (`kab_profiles`.profile, `panelProject.offsetName`, `room.offsetName`). 8 goldens byte-identical; check.mjs green; Doors + Panels DXF export verified in-app (OFFSET_A..F + PROFILE 118 verts/sheet). **Next: toolpath template matched by the same preset name.**
- **Phase 4 billing E2E CONFIRMED BY EDNEI** 2026-07-12 (ROADMAP *jj*): he ran the 4242 loop himself — subscribe→charged→**plan flipped**→cancel worked. Full chain validated (app→function→Checkout→webhook→plan→portal). No customer email arrived — EXPECTED: Stripe email toggles were off (now enabled: Customer emails "Successful payments" + Subscriptions "upcoming renewals"/"card payments fail") AND **test/sandbox mode doesn't deliver customer emails to real inboxes** (dashboard-only until LIVE). A Kabacal-branded confirmation email = webhook + our own SMTP (same dep as magic-links, deferred). His subscription customer = edneilacerda@gmail.com (checkout maps the right email). Remaining Phase-4 follow-ups unchanged below.
- **Phase 4 billing WIRED & PROVEN — one payment click + one redeploy from done** 2026-07-12 (ROADMAP *gg*/*hh*/*ii*): Upgrade buttons live (`fb9ea00`). Ednei re-pasted the correct-account `sk_test` key → **`create-checkout-session` now returns HTTP 200 with a real Stripe Checkout URL**; drove it to the hosted page which renders "Subscribe to Kabacal Starter £15/mo", email pre-filled — the whole chain works. **The final "Pay with test card 4242" click is intentionally left to Ednei** (completing a subscription/payment is a human action; Stripe even shows an "I am an AI agent" attestation there). Then the webhook flips plan→starter (verify), and cancel→beta. Two known follow-ups, neither blocking new users: (1) **redeploy the self-heal patch** (`404b9f7`) so the 2 EXISTING accounts that got orphan `billing_customers` rows from the earlier wrong key also work — a brand-new account already checks out fine (proven on a clean iso-a account); redeploy was blocked all session by a degraded Supabase Edge Functions dashboard ("Deploy status unavailable", SQL/function editors wouldn't hydrate). (2) clear the 2 orphan rows (iso-a `cus_UrrZ…`, iso-b `cus_Urt4…`) once SQL editor is back, or let self-heal replace them. Redeploy path when the UI recovers: dashboard editor, or `npx supabase functions deploy create-checkout-session create-portal-session --project-ref rvmyalrtoblxmxciiovd`.
- Superseded 2026-07-11: **Phase 4 TEST MODE, one paste away** (ROADMAP *ff*): Stripe sandbox live (3 placeholder prices captured, webhook `kabacal-webhook` → the function, 5 events) and all 3 Edge Functions **deployed** (webhook JWT off). Secret-store writes are human-only (classifier policy) → **Ednei pastes the 4 secrets** (table in supabase/README.md §Phase 4), then agent wires ☁ Upgrade buttons + 4242 test-card E2E. D5 (real prices) still open — placeholders £15/£29/£59.
- Superseded 2026-07-11: **SaaS Phase 4 (Stripe billing) PREPARED** (ROADMAP *ee*) — everything buildable without Ednei's Stripe account: migration 0002 applied to hosted (billing_customers/subscriptions, read-only RLS, webhook = only writer), 3 Edge Functions written in `supabase/functions/` (webhook signature-verified `verify_jwt=false`; checkout + portal OWNER-only with price allowlist + pinned CORS), app enforces `status='suspended'` (banner, saves blocked, reads allowed — verified in-app), isolation suite extended to **13 checks, all green vs hosted** (T10–T12 billing). Go-live = Ednei's ~30-min runbook in supabase/README.md (Stripe test account → prices → deploy → secrets → webhook) + D5 amounts; then the agent wires Upgrade buttons + test-card E2E.
- Superseded 2026-07-11: **SaaS Phase 3 (workshop settings Push/Pull) LIVE (dark)** (ROADMAP *dd*): ☁ modal → "Workshop settings" section with ⇧ Push / ⇩ Pull (in-modal confirms). Raw localStorage strings of the 8 business keys (`kab_prices/pricecfg/custom_mats/company/tooldb/doorTpl/offcutTpl/tp_templates`) mirror into `account_settings.settings.keys`; `kab_seq` max-merges (never backwards; atomic RPC deferred until 2-member accounts); Pull reloads with the on-screen job carried via sessionStorage stash. E2E vs hosted green (push → diverge → pull restored company/prices, merged seq, carried job, toast shown). **Risk 3 is mitigated from the moment Ednei pushes his real settings once.** Flag-off pristine. Before beta invites: custom SMTP (unchanged).
- Superseded 2026-07-11: **SaaS Phase 2 (cloud jobs) LIVE (dark)** (ROADMAP *cc*): ☁ modal gained Save to cloud / Update / Save-as-new / Open from cloud (list, click to load) / Archive; `jobs.job_json` = exact `buildFastCnc()`, opening via `loadFastCnc` (tolerant reader); `cloudJob` tracks the loaded cloud row; **welcome gate** fronts the sign-in page on boot when cloud is on + signed out (once per tab session, skippable) — Ednei's requested "page before the app" without breaking local-first. E2E vs hosted green (save→update same row→wipe→open restores→save-as-new→archive); fixed a stuck-busy bug in `cloudOpenJob` found by the E2E. Flag-off still pristine (no chip/CDN/£180). Ednei's real magic-link test PASSED on the live app. Before beta invites: custom SMTP (unchanged).
- Superseded 2026-07-11: **SaaS Phase 1 LIVE (dark)** (ROADMAP *aa*): hosted project `rvmyalrtoblxmxciiovd` created by Ednei and configured via his browser session (migration run in SQL editor, **signups disabled**, Site URL + localhost redirects, 3 users: Ednei + iso-a/b test fixtures); **all 9 isolation tests + sanity PASSED against the hosted DB**; app inlines `CLOUD_DEFAULTS` (URL + publishable key — public by design) so `kab_cloud={"enabled":true}` is the whole device opt-in; in-app E2E green (real sign-in → chip shows workshop/beta/owner via RLS → sign-out; stranger OTP refused). Flag-off still byte-identical. **Before inviting any beta shop: custom SMTP** (built-in mailer = org members only, link-only template). Next: Phase 2 (cloud jobs).
- Superseded 2026-07-11: Phase 1 shipped dark (ROADMAP *z*) — see the *aa* entry above; the Docker/local-stack blocker became irrelevant (tests ran against hosted).
- Nothing mid-work. **SaaS Phase 0 foundation** shipped 2026-07-11 (ROADMAP *y*): `docs/SAAS.md` (staged phases 0–5, data model, RLS plan, secrets policy, sensitive-config audit, Stripe-later, beta plan), `supabase/migrations/0001_saas_foundation.sql` (accounts/account_members/jobs/account_settings, deny-by-default RLS + column grants), `supabase/README.md` (apply runbook + 9 isolation acceptance tests), `.env` hygiene in `.gitignore`, and an **inert** `kab_cloud` flag stub in `index.html` (`CLOUD_PHASE=0` — zero behaviour change, goldens untouched). No Supabase project exists yet; **Phase 1 (optional login) waits on Ednei's D1–D4 in docs/SAAS.md** — login is deliberately NOT implemented until the auth method + data decisions are approved and the migration has passed isolation tests on a local stack.
- Nothing mid-work. Panels layout + Openings-into-Wall shipped 2026-07-11 (ROADMAP *w*, UI/CSS only): `.pn-insp` height pinned to the drawing area (`calc(100vh−282px)`, min 340) so a tall accordion inspector no longer pushes the page / scrolls the drawing away — only the inspector scrolls (reproduced at 650px: layout 616→368). Top-level Openings tab removed → categories = Room/Wall/Panel/Corners/Export; the opening editor+list+add now lives in the **Wall** tab (helpers `pnFrontOpeningsAcc`/`pnPlanOpeningsAcc`; selecting/adding focuses Wall). Goldens byte-identical (WALL_LAYOUT 3428 / PANELS_18mm 10038), check.mjs green, doors basket intact (£3228), no console errors.
- Nothing mid-work. Panels ACCORDION round shipped 2026-07-11 (ROADMAP *v*): Doors-style accordion inside every Panels category (Room = Project Standards / Room Setup / Skirting & Sill / Offsets / Pricing-last; Wall gained the per-end Auto/Through/Butt/Overlap "Wall ends", lost side rules; Panel gained per-panel sides incl. **Gap(−th)/Overlap(+th)** + per-panel skirting UI; Corners = global only). Offset **scope Project vs Room** (`pnProjLines` + `room.linesScope`, additive persistence `kab_panels.proj` / `doc.panelProject`; legacy rooms infer 'room' when they have enabled lines). **Thickness 12/18/22** replaces the Room material select (syncs plan panel-layer pt). Offset **auto-scroll bug fixed** (renderPanels preserves inspector scroll + focus; reproduced on live 863→0, fixed 670→670). Overlap visuals: filled band + red **overlap conflict** (same-wall piece pass + cornerInfo.conflict in compile), allowed never blocked. check.mjs green with new engine tests; goldens byte-identical (WALL_LAYOUT 3428 / PANELS_18mm 10038 browser-compared from clean state); doors basket intact (£300/£360).
- Nothing mid-work. Panels UI cleanup shipped 2026-07-10 (ROADMAP *u*, UI/SVG/CSS only): removed "Prices are in the Quote tab"; panorama per-wall line trimmed to `wall X[ · panel Y]` (no repeated height / "click to open", fixes overlap); Front-view bottom = one clean `Wall width: {measured} · Panel width: {physical}` (no repeated panel size, window edges no longer add a bottom dimension); panel letters render as a bold-ink tspan (Front + panorama); `.pn-itab` tabs restyled like the Doors editing buttons. Goldens byte-identical (WALL_LAYOUT + PANELS_18mm browser-compared), only `index.html` changed, check.mjs green, no pricing impact.
- Nothing mid-work. Wall-Layout-DXF detail + compact panorama + door-preview-frame shipped 2026-07-10 (ROADMAP *t*): `pnWallLayoutDxf` now insets room offset/pocket lines (A–G) inside each cavity like the Sheet DXF (offsets/pockets export in the Wall Layout DXF) + `overlap +N` labels; panoramic gaps cut >half (Wall Layout DXF `GAP 250→110`/`ROOMGAP 700→300`, `pnPanoSvg gap 420→160`, wall-label font auto-fits); `offsetPreview` always draws the frame/cavity (uneven-aware) so a per-side frame (bottom 305) is no longer invisible. Only `GOLDEN_WALL_LAYOUT` moves (positions only, regenerated); `GOLDEN_PANELS_18mm` byte-compared identical, Sheet DXF/NC/quote untouched; check.mjs green. No pricing impact.
- Nothing mid-work. Door-adjacent vertical-panel fix shipped 2026-07-10 (ROADMAP *s*, `1742fc3`): `pnZonePieces` now sets a vertical zone's side margin by what ABUTS it (door → `doorAllow`, object → frame, wall edge → sideL/R, else joint), mirroring `pnWallSpans`. A vertical panel butting a door previously kept only the 40mm joint, so its shakers ran into the door clearance. Physical piece unchanged → nesting/price identical; all 8 sheet goldens + `GOLDEN_WALL_LAYOUT` byte-identical; new check.mjs test. Repro: real Drive file JamesTEST.fastcnc / Ensuite 3 / Wall 2. Also shipped 2026-07-10: inspector 6-tab reorg (*r*) + manual Overlap end (*q*).
- Nothing mid-work. Panels-inside / labels / horizontal-Wall-DXF round shipped 2026-07-10 (ROADMAP *n*): panels now always render INSIDE the room via ray-cast point-in-polygon interior detection (`pnPlanEdgeFrame`/`pnPlanChain`/`pnPointInPoly`) — fixes non-convex rooms flipping panels outside; manual `plan.flipInside` ("Flip panel side"). Wall size vs panel size separated in tabs/panorama/front-inspector (wall = measured, panel = shortened). Wall Layout DXF is now HORIZONTAL/panoramic (golden `GOLDEN_WALL_LAYOUT.dxf` regenerated 3501→3428). Rendering-only + one intended golden → sheet goldens byte-identical (GOLDEN_PANELS byte-compared), check.mjs green.
- Nothing mid-work. Corner-rule/window/DXF round shipped 2026-07-10 (ROADMAP *l*): **corner rule CONFIRMED & flipped** — through = frame+pt, butt = normal frame + pt gap (RESOLVES the open question from *j*/*k*; Ednei confirmed); window overlap fixed properly (no lower panel when sill ≥ band top; new default bottom = panel height); **cross-corner shaker match** `room.cornerMatch` (opt-in, pins corner shakers to a shared width across L/U); **second DXF type** Wall Layout DXF (`pnWallLayoutDxf`, `⬇ Wall DXF`) separate from the Sheet DXF. Engine changes affect plan rooms only / are opt-in → 8 sheet goldens byte-identical (7 byte-compared) + new `GOLDEN_WALL_LAYOUT.dxf` + new check.mjs tests. Still deferred: exact whole-run shaker seam-equality across a 90° turn (runs are linear — cornerMatch pins the end cells, the visible corner).
- Nothing mid-work. 2D-builder settings/simplify round shipped 2026-07-09 (ROADMAP *k*): removed Mode/Keep-90/Pan visible controls; Top-view inspector now exposes the full wall panel rules + Room defaults (`pnWallPanelSections`); **Panel ON/OFF per wall** (`edge.noPanel`→`wall.noPanel`, excluded from quote/DXF/nesting, save/load + granular undo); prices removed from builder views (Quote tab only); **corner winding foundation** (`cornerInfo.winding` + `plan.cornerMode` auto|winding, opt-in); tighter selection hit radius; multi-opening add fixed; smaller clearance mark. Engine changes are default-off → **all 8 goldens byte-identical** (7 byte-compared) + new check.mjs tests. **STILL OPEN for Ednei** (carried from *j*): the corner frame-allowance direction — item 4/5's restatement (through=frame+pt) is the REVERSE of the confirmed rule (butt=frame+pt+shortening); `winding` mode is opt-in pending confirmation of the exact ACW full-length panel. Cross-corner shaker matching across `corner`/butt + H↔V joints still deferred.
- Nothing mid-work. 2D-builder refinement round shipped 2026-07-09 (ROADMAP *j*): mitred continuous walls at any angle (`pnPlanMiterOut` — no overlap/gap), corner naming (Through/Butt), subtle front-view clearance, measure label offset + full-wall-end snap, middle-mouse pan, Delete selected unlocked item, granular builder Ctrl+Z (isolated from doors undo). Top/front-view rendering + interaction only → **all goldens byte-identical** (7 byte-compared). **OPEN QUESTION for Ednei**: item 2 restated the corner frame-allowance the REVERSE of the confirmed/golden-locked rule (they said through=frame+pt, butt=normal; engine does butt=frame+pt+shortening, through=normal). Not flipped without confirmation — flipping changes panel cell layout + GOLDEN_PANELS. Also deferred: cross-corner shaker matching ACROSS `corner`/butt + horizontal↔vertical joints (engine already matches within `joint`-chained runs).
- Nothing mid-work. Post-test bugfix round shipped 2026-07-08 (ROADMAP *i*): window-overlap fix (band notches to floor when a lower panel exists — regenerated GOLDEN_PANELS_18mm.dxf 10038, other 7 goldens + QUOTE_mixed identical), Measure CTM-exact, deduped/transparent corner locks, door breaks the top-view panel band, front view shows full measured length + clearance + calmer colours, closed exterior joins, Top/Front view labels. Phase 3 (columns/returns, multi-hop squaring, 2.5D) still open.
- Nothing mid-work. 2D builder usability pass shipped 2026-07-08 (ROADMAP *h*): 100mm default wall, inside-face reference, visible "corner clearance" gap (wall full length, panel inset), click-to-toggle lock, calmer keep-90° (drag moves only grabbed node; length-edit one-hop excludes anchor — fixed a self-cancelling bug), non-trapping node drag, editable through/butt per corner, wall-band draw preview. All plan-view/interaction only — 8 goldens byte-identical. Phase 3 (full columns/returns, multi-hop constraint solve, optional 2.5D) not started. Active sessions: claim `.session.lock`.

## Resolved

- **Per-panel "Vertical" = physical vertical panel** (Ednei confirmed 2026-07-08). Implemented via `wall.vZones` (physical 1200×3000 10x4 piece; wall auto-refills around it; mixed orientation). The old `panelOv.dir` shaker-style flip stays in the engine + its check test but is off the orientation UI. Earlier stash `round4-vZones-superseded-by-83ebf1e` is now obsolete (superseded by the shipped clean version) — safe to drop.

## Decision log

- 2026-07-11 · **D1–D4 answered (Ednei)**: magic-link only (password addable later, additive); embedded FAST CNC prices stay for now (revisit post-Phase 3); Phase-3 sync = ALL business keys; repo stays public. Recorded in docs/SAAS.md §Decisions.
- 2026-07-11 · **SaaS = staged, local-first** (docs/SAAS.md is canonical): cloud is an additive sync/identity layer — engines stay in-page, localStorage + `.fastcnc` stay primary, app fully works logged-out forever. Stack: Supabase (magic-link auth recommended, RLS as the only tenant boundary, invite-only beta with signups disabled), Stripe only at Phase 4 via Edge Functions (no secrets ever in this repo — GitHub Pages can't hold them). `jobs.job_json` = exact `.fastcnc` payload; `account_settings.settings` = one jsonb mirroring the business `kab_*` keys. Phase 0 shipped; each next phase gated (Phase 1 on D1–D4 + isolation tests).
- 2026-07-08 · **2D builder Phase 2** — corner inference (shorter butts into longer), thickness-driven physical shortening (−pt per butt end) + `cornerTh`=panel-thickness corner allowance (frame+pt); endpoint locks, keep-90° (one-hop), draggable openings, teal panel layer. NON-plan rooms: `cornerTh===material th` → byte-identical. Corner maths locked by exact-mm engine tests (no new binary golden).
- 2026-07-08 · **2D room builder** (`room.plan` → `pnPlanCompile` → `room.walls`): SVG top-down, additive, no plan = unchanged. Plan is editor source; walls stay the engine's single input. SVG-first by decision (no Three.js; performance). All 8 goldens byte-identical.
- 2026-07-08 · **Vertical zones** (`wall.vZones`) = real physical vertical panels (mixed orientation, band auto-refill, 40/40); **quote notes** (`project.quoteNotes`) on the customer PDF, separate from internal wall/panel notes. All 8 goldens byte-identical.
- 2026-07-08 · **Per-wall skirting** added (`wall.skirt`), resolver `D.skirtFor` (panel>wall>room); per-panel prepared (`wall.panelSkirt`), no UI. Old-import skirting now maps block+per-part faithfully (305→225). Notes (`wall.notes`, `wall.panelNotes`) are inert (no geometry/price). All goldens byte-identical.
- 2026-07-08 · **3D / wall-builder = architecture review only** (no code this session). Recommendation on record in ROADMAP-adjacent report / chat: SVG top-down (2D) first feeding the existing PN engine; Three.js deferred. See the review before starting.
- 2026-07-07 · **Doors is the default mode** on boot (`kab_mode` no longer restored). Panels stays one click away.
- 2026-07-07 · **Edging pricing deferred** (user's call) until pricing consolidation.
- 2026-07-07 · Panels shipped (engine + quote + DXF). **Panels CAM/offcuts = Phase 2**, not started.
- 2026-07-06 · **Do NOT split `index.html` into modules** until a trigger fires: 2+ genuine parallel writers, file ≳600KB, or the pricing-DB build-out. When it fires: extract the CAM engine first, classic `<script src>` (no build), one zero-behaviour commit per seam, goldens byte-stable throughout.
- 2026-07-01 · The **Website pricing formula** (per door: £25 + £139/m², min £20) comes from the fastcnc.co.uk reprice study (Plain Shaker + Flat = avg JMF × cutmy). Full write-up belongs in `docs/PRICING.md` (planned).
- Standing · Single file, no build, no dependencies; additive persistence; `KABACAL_RULES.md` is the rule book; PT welcome in rules/roadmap.

## Next (recommended order)

1. **Air-cut dry run** on the Pegasus → flip goldens to known-good (highest-value single morning available).
2. **Verify + fix toolpath scope staleness** (risk 2).
3. **Auto-backup for the `kab_*` business keys** (risk 3).
4. **SaaS next**: Ednei uses Phases 1–3 for real for ~a week (tenant #1) + pushes his settings once; custom SMTP (Resend free / fastcnc mailbox) before any beta invite; Stripe go-live only via the supabase/README.md runbook + D5 (code fully prepared). Re-run `tools/saas-isolation-test.mjs` (now 13 checks) after any policy change.

## Doc system

`AGENTS.md` = entry point for every model. **Phase 2 done (2026-07-07):** `docs/TESTING.md`; goldens expanded to 13 files (rich doors DXF ×4 thicknesses, panels DXF, toolchange NC ×2, quote snapshots ×3); `examples/` (3 loadable jobs + takeoff sample, mixed one round-trip-verified); `.githooks/pre-commit` (enable per clone: `git config core.hooksPath .githooks`). Quote baskets do NOT yet cover formula mode / overrides / custom materials — extend when touching those paths. **Phase 3 done (2026-07-07):** `docs/ARCHITECTURE.md` (state registry, `.fastcnc` schema, marker blocks, split map), `docs/CONTRACT-CAM.md`, `docs/CONTRACT-DXF.md`, `docs/PRICING.md`; subagents demoted to wrappers around them; KABACAL_RULES got a TOC. The `.claude/` folder is now pure convenience — deleting it loses no knowledge. **Phase 4 done (2026-07-07):** NEST/OFFCUT/CAM engine marker blocks with executed node tests in `check.mjs` (the PN pattern); engines are split-ready. **SaaS Phase 0 (2026-07-11):** `docs/SAAS.md` + `supabase/` (migration draft, apply runbook + isolation tests, `.env.example`).
