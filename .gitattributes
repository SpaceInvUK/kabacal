# Golden files: byte-exact, never EOL-normalise (NC is CRLF by machine contract)
tests/golden/*.nc -text
tests/golden/*.dxf -text
tests/golden/*.json -text

# Example jobs + hook: keep LF
examples/*.json text eol=lf
examples/*.txt text eol=lf
.githooks/pre-commit text eol=lf
